// Web-specific stub initializer. Keeping this separate makes intent explicit.
Future<void> firebaseInitialize() async {
  // For web we will keep using the local stubs in this project to avoid
  // firebase_web interop issues unless you explicitly opt in.
  return;
}
