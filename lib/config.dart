// Simple runtime toggle to switch between local stubs and real Firebase.
// Set to `true` to enable real Firebase initialization (requires the
// Firebase configuration in `lib/firebase_options.dart` and platform
// setup files). Keep `false` for offline/stubbed testing.
const bool useFirebase = false;
